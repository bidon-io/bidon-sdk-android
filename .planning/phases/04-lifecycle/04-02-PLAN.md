---
phase: 04-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bidon/src/main/java/org/bidon/sdk/ads/cache/denis/lifecycle/CancellationManager.kt
autonomous: true

must_haves:
  truths:
    - "showAd() cancels ongoing CPM processing to avoid wasted network requests"
    - "Successfully loaded ads remain in cache even if auction cancelled mid-flight"
    - "One cancel per auction (subsequent showAd() calls just pull from cache)"
  artifacts:
    - path: "bidon/src/main/java/org/bidon/sdk/ads/cache/denis/lifecycle/CancellationManager.kt"
      provides: "Auction cancellation coordination for showAd() triggered cancellation"
      exports: ["CancellationManager"]
  key_links:
    - from: "CancellationManager"
      to: "Job.cancel()"
      via: "cancelAuction() method"
      pattern: "Job\\??\\.cancel\\(\\)"
---

<objective>
Create cancellation manager for showAd()-triggered auction cancellation.

Purpose: Implement auction cancellation coordination that cancels ongoing RTB + CPM processing when showAd() is called, while keeping successfully loaded ads in cache. Uses Job.cancel() for cancellation signaling with proper state tracking.

Output: CancellationManager class for auction job lifecycle and cancellation coordination.
</objective>

<execution_context>
@/Users/glavatskikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glavatskikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lifecycle/04-CONTEXT.md
@.planning/phases/04-lifecycle/04-RESEARCH.md

Key decisions from CONTEXT.md:
- Cancel all ongoing processing (RTB + CPM) when showAd() is called
- Use coroutine Job.cancel() for cancellation signaling
- Keep successfully loaded ads in cache even if auction cancelled mid-flight
- One cancel per auction (subsequent showAd() calls just pull from cache without re-cancelling)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CancellationManager</name>
  <files>
    bidon/src/main/java/org/bidon/sdk/ads/cache/denis/lifecycle/CancellationManager.kt
  </files>
  <action>
Create CancellationManager following the research patterns from 04-RESEARCH.md.

```kotlin
package org.bidon.sdk.ads.cache.denis.lifecycle

import kotlinx.coroutines.Job
import org.bidon.sdk.logs.logging.impl.logInfo

/**
 * Manages auction job lifecycle and cancellation coordination.
 *
 * Handles showAd()-triggered cancellation:
 * - Tracks current auction job by auctionId
 * - Cancels ongoing RTB + CPM processing when showAd() is called
 * - Ensures one cancel per auction (idempotent cancellation)
 *
 * CRITICAL: Successfully loaded ads remain in cache after cancellation.
 * Cancellation only stops ONGOING processing - completed operations are preserved.
 *
 * Thread-safety: Uses synchronized blocks for atomic state updates.
 */
internal class CancellationManager {
    /**
     * Current auction job (nullable when no auction running).
     */
    private var currentAuctionJob: Job? = null

    /**
     * Current auction ID for tracking.
     */
    private var currentAuctionId: String? = null

    /**
     * Lock for thread-safe state updates.
     */
    private val lock = Any()

    /**
     * Register a new auction job.
     *
     * Called when starting a new auction in coordinateAuction().
     * Any previous auction job is NOT cancelled - it may have loaded ads into cache.
     *
     * @param auctionId Unique auction identifier
     * @param job Coroutine job for the auction
     */
    fun registerAuction(auctionId: String, job: Job) {
        synchronized(lock) {
            // Log if replacing existing auction (shouldn't happen normally)
            if (currentAuctionJob?.isActive == true && currentAuctionId != auctionId) {
                logInfo(TAG, "Warning: registering new auction while previous still active " +
                    "(prev=$currentAuctionId, new=$auctionId)")
            }

            currentAuctionId = auctionId
            currentAuctionJob = job
            logInfo(TAG, "Auction registered: auctionId=$auctionId")
        }
    }

    /**
     * Cancel auction if matching auctionId.
     *
     * Called from showAd() to stop ongoing processing for the shown ad's auction.
     * Uses auctionId matching to avoid cancelling unrelated auctions.
     *
     * Idempotent: calling twice with same auctionId only cancels once.
     *
     * @param auctionId Auction ID of the ad being shown
     * @return true if auction was cancelled, false if no match or already completed
     */
    fun cancelIfMatching(auctionId: String): Boolean {
        synchronized(lock) {
            // Only cancel if auctionId matches current auction
            if (currentAuctionId != auctionId) {
                logInfo(TAG, "No cancellation: auctionId mismatch (showing=$auctionId, current=$currentAuctionId)")
                return false
            }

            val job = currentAuctionJob
            if (job == null || !job.isActive) {
                logInfo(TAG, "No cancellation: auction already completed (auctionId=$auctionId)")
                return false
            }

            // Cancel the auction job
            logInfo(TAG, "Cancelling auction: auctionId=$auctionId")
            job.cancel()

            // Clear state (prevent double cancellation)
            currentAuctionJob = null
            currentAuctionId = null

            return true
        }
    }

    /**
     * Cancel current auction unconditionally.
     *
     * Called when ad instance is destroyed (destroyAd() / clear()).
     * Cancels any running auction regardless of auctionId.
     *
     * @return true if an active auction was cancelled
     */
    fun cancelCurrent(): Boolean {
        synchronized(lock) {
            val job = currentAuctionJob
            if (job == null || !job.isActive) {
                logInfo(TAG, "No active auction to cancel")
                return false
            }

            logInfo(TAG, "Cancelling current auction: auctionId=$currentAuctionId")
            job.cancel()

            // Clear state
            currentAuctionJob = null
            currentAuctionId = null

            return true
        }
    }

    /**
     * Check if auction is currently running.
     *
     * @return true if an active auction job exists
     */
    fun isAuctionRunning(): Boolean {
        synchronized(lock) {
            return currentAuctionJob?.isActive == true
        }
    }

    /**
     * Get current auction ID (for logging/debugging).
     *
     * @return Current auction ID or null if no auction running
     */
    fun getCurrentAuctionId(): String? {
        synchronized(lock) {
            return if (currentAuctionJob?.isActive == true) currentAuctionId else null
        }
    }

    /**
     * Clear state when auction completes normally.
     *
     * Called when auction finishes successfully (not cancelled).
     * Prevents stale state from affecting future operations.
     *
     * @param auctionId Auction ID that completed
     */
    fun onAuctionCompleted(auctionId: String) {
        synchronized(lock) {
            if (currentAuctionId == auctionId) {
                logInfo(TAG, "Auction completed normally: auctionId=$auctionId")
                currentAuctionJob = null
                currentAuctionId = null
            }
        }
    }

    companion object {
        private const val TAG = "CancellationManager"
    }
}
```
  </action>
  <verify>
Verify file compiles:
```bash
./gradlew :bidon:compileReleaseKotlin 2>&1 | grep -E "(error|BUILD)"
```
  </verify>
  <done>
CancellationManager is created with:
- registerAuction() for tracking auction jobs
- cancelIfMatching() for showAd()-triggered cancellation with auctionId matching
- cancelCurrent() for unconditional cancellation (destroyAd)
- Thread-safe synchronized blocks for state updates
- Idempotent cancellation (calling twice is safe)
  </done>
</task>

</tasks>

<verification>
1. File compiles without errors: `./gradlew :bidon:compileReleaseKotlin`
2. CancellationManager has registerAuction() method
3. CancellationManager has cancelIfMatching(auctionId) method
4. CancellationManager uses synchronized blocks for thread safety
5. CancellationManager uses Job.cancel() for cancellation signaling
</verification>

<success_criteria>
- CancellationManager provides auction job lifecycle tracking
- showAd() can cancel matching auctions via cancelIfMatching()
- Idempotent cancellation (safe to call multiple times)
- Thread-safe state management
</success_criteria>

<output>
After completion, create `.planning/phases/04-lifecycle/04-02-SUMMARY.md`
</output>
