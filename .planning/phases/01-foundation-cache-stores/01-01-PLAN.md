---
phase: 01-foundation-cache-stores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/CacheEntry.kt
  - bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/TtlConfig.kt
autonomous: true

must_haves:
  truths:
    - "Cache entries track creation time using monotonic clock"
    - "TTL expiration is calculated correctly (30 minutes)"
    - "Expired entries are detectable without side effects"
  artifacts:
    - path: "bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/CacheEntry.kt"
      provides: "Generic cache entry wrapper with timestamp and eCPM"
      contains: "data class CacheEntry"
    - path: "bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/TtlConfig.kt"
      provides: "TTL configuration and expiration checking"
      contains: "SystemClock.elapsedRealtime"
  key_links:
    - from: "CacheEntry.kt"
      to: "TtlConfig.kt"
      via: "expiresAt calculation uses TtlConfig.TTL_MILLIS"
      pattern: "TtlConfig\\.TTL_MILLIS"
---

<objective>
Create foundational cache entry data model with TTL tracking using monotonic time source.

Purpose: Provides the core data structure for both ReadyToShowCache and RtbPayloadCache. Using SystemClock.elapsedRealtime() ensures TTL works correctly even when system time changes. This is the foundation that all cache stores depend on.

Output: CacheEntry.kt (generic wrapper) and TtlConfig.kt (TTL constants and expiration logic)
</objective>

<execution_context>
@/Users/glavatskikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glavatskikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cache-stores/01-RESEARCH.md

# Existing codebase patterns
@bidon/src/main/java/org/bidon/sdk/ads/cache/AdCache.kt
@bidon/src/main/java/org/bidon/sdk/auction/models/AuctionResult.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TtlConfig with monotonic time utilities</name>
  <files>bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/TtlConfig.kt</files>
  <action>
Create TtlConfig.kt in the denis/stores package with:

1. Object TtlConfig containing:
   - TTL_MILLIS constant = 30 * 60 * 1000L (30 minutes in milliseconds)
   - SWEEP_INTERVAL_MILLIS constant = 5 * 60 * 1000L (5 minutes for periodic sweep)
   - fun now(): Long = SystemClock.elapsedRealtime() - monotonic time source
   - fun expiresAt(): Long = now() + TTL_MILLIS - calculates expiration timestamp
   - fun isExpired(expiresAt: Long): Boolean = now() > expiresAt - checks if expired

Use android.os.SystemClock for monotonic time (NOT System.currentTimeMillis - it can jump with time changes).

Mark as internal since this is SDK-internal code.

Package: org.bidon.sdk.ads.cache.denis.stores
  </action>
  <verify>
File exists and compiles: ./gradlew :bidon:compileReleaseKotlin 2>&1 | grep -E "(BUILD|error)"
  </verify>
  <done>
TtlConfig object exists with TTL_MILLIS=1800000, now() returns SystemClock.elapsedRealtime(), isExpired() correctly compares timestamps
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CacheEntry generic data class</name>
  <files>bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/CacheEntry.kt</files>
  <action>
Create CacheEntry.kt in the denis/stores package with:

1. Generic data class CacheEntry<T> containing:
   - val value: T - the cached item (LoadedAd or RtbPayload)
   - val ecpm: Double - eCPM for comparison in duplicate detection
   - val expiresAt: Long - timestamp when entry expires (from TtlConfig.expiresAt())
   - val demandId: String - demand network identifier for lookup
   - val auctionId: String - auction identifier for tracking (STAT-02)

2. Extension function CacheEntry<*>.isExpired(): Boolean that delegates to TtlConfig.isExpired(expiresAt)

3. Companion object with factory function:
   - fun <T> create(value: T, ecpm: Double, demandId: String, auctionId: String): CacheEntry<T>
     Creates entry with expiresAt set to TtlConfig.expiresAt()

Mark as internal since this is SDK-internal code.

Package: org.bidon.sdk.ads.cache.denis.stores

Note: Do NOT use System.currentTimeMillis() anywhere - always use TtlConfig for time operations.
  </action>
  <verify>
File exists and compiles: ./gradlew :bidon:compileReleaseKotlin 2>&1 | grep -E "(BUILD|error)"
  </verify>
  <done>
CacheEntry data class exists with value, ecpm, expiresAt, demandId, auctionId fields. Factory function creates entries with correct expiration. isExpired() extension uses TtlConfig.
  </done>
</task>

</tasks>

<verification>
1. Both files compile without errors:
   ./gradlew :bidon:compileReleaseKotlin

2. Package structure is correct:
   ls bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/

3. TtlConfig uses SystemClock (grep for verification):
   grep -n "SystemClock.elapsedRealtime" bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/TtlConfig.kt

4. No System.currentTimeMillis() usage (anti-pattern check):
   grep -r "currentTimeMillis" bidon/src/main/java/org/bidon/sdk/ads/cache/denis/ || echo "OK: No currentTimeMillis found"
</verification>

<success_criteria>
- CacheEntry.kt and TtlConfig.kt exist in denis/stores package
- TtlConfig uses SystemClock.elapsedRealtime() for monotonic time
- CacheEntry has all required fields (value, ecpm, expiresAt, demandId, auctionId)
- isExpired() correctly detects expired entries
- Build passes: ./gradlew :bidon:compileReleaseKotlin
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cache-stores/01-01-SUMMARY.md`
</output>
