---
phase: 01-foundation-cache-stores
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt
autonomous: true

must_haves:
  truths:
    - "Loaded ads can be stored and retrieved by demandId"
    - "Concurrent put/get/remove operations are thread-safe"
    - "Expired entries are removed on access (lazy eviction)"
    - "Cache respects capacity limit (1-3 entries)"
    - "getBest() returns ad with highest eCPM"
  artifacts:
    - path: "bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt"
      provides: "Thread-safe storage for loaded ads ready to show"
      contains: "object ReadyToShowCache"
      exports: ["put", "get", "remove", "getBest", "getAll", "clear", "size", "isEmpty"]
  key_links:
    - from: "ReadyToShowCache.kt"
      to: "CacheEntry.kt"
      via: "stores CacheEntry<AuctionResult>"
      pattern: "CacheEntry<AuctionResult>"
    - from: "ReadyToShowCache.kt"
      to: "TtlConfig.kt"
      via: "lazy eviction checks expiration"
      pattern: "isExpired\\(\\)"
---

<objective>
Implement ReadyToShowCache singleton for storing loaded ads ready for display.

Purpose: This cache stores fully loaded ads (AuctionResult) that can be shown immediately on showAd() call. It enables warm start optimization - when cache is not empty, onAdLoaded fires immediately. The cache is application-wide (singleton) to persist between ad instances.

Output: ReadyToShowCache.kt with thread-safe ConcurrentHashMap storage, lazy eviction, and capacity limits
</objective>

<execution_context>
@/Users/glavatskikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glavatskikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cache-stores/01-RESEARCH.md

# Depends on Plan 01 artifacts
@bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/CacheEntry.kt
@bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/TtlConfig.kt

# Existing types to use
@bidon/src/main/java/org/bidon/sdk/auction/models/AuctionResult.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ReadyToShowCache singleton</name>
  <files>bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt</files>
  <action>
Create ReadyToShowCache.kt as a Kotlin object (singleton) with:

1. Private storage:
   - private val cache = ConcurrentHashMap<String, CacheEntry<AuctionResult>>()
   - Key is demandId (String)

2. Capacity configuration:
   - private const val DEFAULT_CAPACITY = 3
   - private var capacity = DEFAULT_CAPACITY
   - fun setCapacity(newCapacity: Int) to configure (range 1-10, default 3)

3. Core operations with lazy eviction:

   fun put(entry: CacheEntry<AuctionResult>) {
     // Evict expired entries first
     evictExpired()
     // Check capacity, evict LRU if at limit
     if (cache.size >= capacity) {
       evictLowestEcpm()
     }
     cache[entry.demandId] = entry
   }

   fun get(demandId: String): AuctionResult? {
     val entry = cache[demandId] ?: return null
     return if (entry.isExpired()) {
       cache.remove(demandId)
       null
     } else {
       entry.value
     }
   }

   fun remove(demandId: String): AuctionResult? {
     val entry = cache.remove(demandId) ?: return null
     return if (entry.isExpired()) null else entry.value
   }

4. Query operations:

   fun getBest(): CacheEntry<AuctionResult>? {
     evictExpired()
     return cache.values.maxByOrNull { it.ecpm }
   }

   fun getAll(): List<CacheEntry<AuctionResult>> {
     evictExpired()
     return cache.values.toList()
   }

   fun isEmpty(): Boolean {
     evictExpired()
     return cache.isEmpty()
   }

   fun size(): Int {
     evictExpired()
     return cache.size
   }

   fun getMaxEcpm(): Double {
     evictExpired()
     return cache.values.maxOfOrNull { it.ecpm } ?: 0.0
   }

5. Clear operation:

   fun clear() {
     cache.clear()
   }

6. Internal eviction helpers:

   private fun evictExpired() {
     val now = TtlConfig.now()
     cache.entries.removeIf { (_, entry) -> now > entry.expiresAt }
   }

   private fun evictLowestEcpm() {
     val lowest = cache.entries.minByOrNull { it.value.ecpm }
     lowest?.let { cache.remove(it.key) }
   }

Mark as internal. Package: org.bidon.sdk.ads.cache.denis.stores

IMPORTANT:
- Use ConcurrentHashMap for thread safety (CACHE-08)
- Call evictExpired() at the start of query operations (CACHE-05 lazy eviction)
- getBest() returns highest eCPM entry for LIFE-01 showAd selection
- Capacity limit of 3 prevents memory exhaustion (CACHE-09)
  </action>
  <verify>
File compiles: ./gradlew :bidon:compileReleaseKotlin 2>&1 | grep -E "(BUILD|error)"
  </verify>
  <done>
ReadyToShowCache singleton exists with:
- ConcurrentHashMap storage (thread-safe)
- put() with capacity check and LRU eviction
- get() with lazy expiration check
- remove() that returns value only if not expired
- getBest() returning highest eCPM entry
- getMaxEcpm() for dynamic pricefloor calculation
- clear() for cleanup
- Capacity configurable (default 3)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add graceful degradation and defensive coding</name>
  <files>bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt</files>
  <action>
Enhance ReadyToShowCache with defensive coding (CACHE-10 graceful degradation):

1. Add peek() method that doesn't remove:

   fun peek(demandId: String): AuctionResult? {
     val entry = cache[demandId] ?: return null
     return if (entry.isExpired()) {
       cache.remove(demandId)
       null
     } else {
       entry.value
     }
   }

2. Add peekBest() that doesn't remove:

   fun peekBest(): AuctionResult? = getBest()?.value

3. Add popBest() that removes and returns:

   fun popBest(): CacheEntry<AuctionResult>? {
     evictExpired()
     val best = cache.entries.maxByOrNull { it.value.ecpm }
     return best?.let {
       cache.remove(it.key)
       it.value
     }
   }

4. Add contains() method:

   fun contains(demandId: String): Boolean {
     val entry = cache[demandId] ?: return false
     return if (entry.isExpired()) {
       cache.remove(demandId)
       false
     } else {
       true
     }
   }

5. Add logging for debugging (using existing SDK logging pattern):

   Import: org.bidon.sdk.logs.logging.impl.logInfo

   Add logging in put():
   logInfo(TAG, "ReadyToShowCache.put: demandId=${entry.demandId}, ecpm=${entry.ecpm}, size=${cache.size}")

   Add logging in evictExpired() when entries are removed:
   if (removed > 0) logInfo(TAG, "ReadyToShowCache evicted $removed expired entries")

   companion object {
     private const val TAG = "ReadyToShowCache"
   }

All operations must be non-throwing - return null/empty on errors rather than exceptions.
  </action>
  <verify>
File compiles: ./gradlew :bidon:compileReleaseKotlin 2>&1 | grep -E "(BUILD|error)"
Verify peek/popBest methods exist: grep -E "fun (peek|popBest)" bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt
  </verify>
  <done>
ReadyToShowCache has:
- peek() for non-destructive read
- peekBest() for checking best ad without removing
- popBest() for showAd() flow (removes and returns best)
- contains() for quick existence check
- Logging for debugging
- No throwing operations (graceful degradation)
  </done>
</task>

</tasks>

<verification>
1. File compiles:
   ./gradlew :bidon:compileReleaseKotlin

2. Singleton pattern verified:
   grep "^internal object ReadyToShowCache" bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt

3. ConcurrentHashMap used:
   grep "ConcurrentHashMap" bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt

4. Lazy eviction present:
   grep "evictExpired" bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt

5. All required methods exist:
   grep -E "fun (put|get|remove|getBest|getAll|clear|size|isEmpty|peek|popBest|contains|getMaxEcpm)" bidon/src/main/java/org/bidon/sdk/ads/cache/denis/stores/ReadyToShowCache.kt
</verification>

<success_criteria>
- ReadyToShowCache.kt exists as singleton object
- Uses ConcurrentHashMap for thread-safe storage
- Implements lazy eviction on all query operations
- Respects capacity limit (default 3)
- getBest()/popBest() return highest eCPM entry
- getMaxEcpm() supports dynamic pricefloor calculation
- All operations are non-throwing (graceful degradation)
- Build passes: ./gradlew :bidon:compileReleaseKotlin
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cache-stores/01-02-SUMMARY.md`
</output>
